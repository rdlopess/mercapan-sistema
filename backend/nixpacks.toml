# Nixpacks configuration for Railway
# Playwright Chromium e instalado explicitamente no build

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm ci",
  "npx playwright install --with-deps chromium"
]

[variables]
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD = "0"
